{
    "version": "0.2.0",
    "configurations": [
        {
            "program": "${workspaceRoot}/cmd/server",
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "args": ["${workspaceRoot}/pigdns.yaml"]
        }
    ]
}