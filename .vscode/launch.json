{
    "version": "0.2.0",
    "configurations": [
        {
            "program": "${workspaceRoot}",
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "args": ["-s", "-d", "pig.io", "-z", "./hack/zone.conf"]
        }
    ]
}